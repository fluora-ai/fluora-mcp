import { Address } from "viem";
import { Money } from "x402/types";
import { PaymentMethods } from "./payment-method.js";
interface ITools {
    signTransaction(amount: number, sellerWalletAddress: string, buyerWalletAddress: string, resource: `${string}://${string}`, paymentMethod: PaymentMethods): Promise<string>;
    verifyAndSettlePayment(amount: Money, address: Address, { facilitatorUrl, paymentHeader, resource, }: {
        facilitatorUrl: string;
        paymentHeader: string;
        resource: `${string}://${string}`;
    }): Promise<any>;
}
export declare class PaymentsTools implements ITools {
    constructor();
    signTransaction(amount: number, sellerWalletAddress: string, buyerWalletAddress: string, resource: `${string}://${string}`, paymentMethod: PaymentMethods): Promise<string>;
    /**
     * Sends a payment transaction to a specified destination address.
     *
     * @param {number} amount - The amount to send
     * @param {string} destinationWalletAddress - The recipient's wallet address
     * @returns {Promise<{resultMessage: string, hash: string}>} An object containing:
     *   - resultMessage: A string describing the transaction result or error
     *   - hash: The transaction hash if successful, empty string if failed
     */
    verifyAndSettlePayment(amount: Money, address: Address, { facilitatorUrl, paymentHeader, resource, paymentMethod, }: {
        facilitatorUrl: `${string}://${string}`;
        paymentHeader: string;
        resource: `${string}://${string}`;
        paymentMethod: PaymentMethods;
    }): Promise<{
        success: boolean;
        message: string;
        responseHeader: string;
        error?: string;
    }>;
}
export {};
//# sourceMappingURL=payment-tools-x402.d.ts.map